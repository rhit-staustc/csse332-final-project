#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

#define MAX_FAMILY_MEMBERS 10

void sleeping_thread_func(void *arg) {
    
    exit(0);
}

void compute_thread_func(void *arg) {
    
    exit(0);
}


int main(int argc, char *argv[]) {
    printf("memory_update_test\n");
    
    
    exit(0);
}
